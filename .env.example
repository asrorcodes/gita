# API base URL. Local: to'g'ridan-to'g'ri backend. Vercel: proxy orqali (CORS / strict-origin xatosiz)
# Local: NEXT_PUBLIC_API_URL=http://89.117.53.108:8081/v1 yoki bo'sh qoldiring
# Vercel: NEXT_PUBLIC_API_URL=/api/v1 (same-origin proxy)
NEXT_PUBLIC_API_URL=http://89.117.53.108:8081/v1

# Proxy backend (faqat server-side, next.config rewrites uchun). Vercel'da kerak bo'lsa o'rnating
# API_BACKEND_URL=http://89.117.53.108:8081
